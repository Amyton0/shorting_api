services:
  api:
    build: .
    container_name: shorting-api
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./:/app
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///data/shorting.db
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload